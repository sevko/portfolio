CHIP Jump {
	IN eq, lt, instruction[3];
	OUT out;

	PARTS:
	Not(in=eq, out=ne);
	Not(in=lt, out=nlt);
	And(a=ne, b=nlt, out=gt);

	Or(a=eq, b=lt, out=le);
	Or(a=eq, b=gt, out=ge);

	Mux8Way(
		a=false,
		b=gt,
		c=eq,
		d=ge,
		e=lt,
		f=ne,
		g=le,
		h=true,
		sel=instruction,
		out=out
	);
}
